name: BASE-05.4 Disabled Creator Weapon-Set Patcher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          echo 'BASE05_4_CREATOR_PATCHER=DISABLED'
          echo 'No repository write permissions remain.'
          echo 'SIGNATURE=Tehkné Solutions'
