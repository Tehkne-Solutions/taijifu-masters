name: BASE-05.2 Disabled Katana Candidate Materializer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          echo 'BASE05_2_MATERIALIZER=DISABLED'
          echo 'No repository write permissions remain.'
          echo 'SIGNATURE=Tehkné Solutions'
