# VM02 C47 — V2 Comparative Playtest Build

C47 turns the validated C46 camera/balance changes into a fresh Windows playtest build for direct comparison with the C45 telemetry baseline.

Baseline: DISCÍPULO, 39.297s player KO, player 12 confirmed hits, CPU 4 confirmed hits, player max combo 6, CPU max combo 0, feedback `too_easy`.

The C47 gate deliberately exports Windows only to reduce iteration time. It produces an external build, launcher, SHA-256 and a compact comparison checklist. Training Rival canonical art remains isolated and non-blocking.

Tehkné Solutions
